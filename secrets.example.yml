openai:
  apiKey: sk-...

flightaware:
  # AeroAPI key for live flight status; omit to turn the feature off.
  apiKey: ...

email:
  # Shared with the Cloudflare email worker (wrangler secret put WEBHOOK_SECRET)
  webhookSecret: change-me
